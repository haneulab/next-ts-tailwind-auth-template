import { type RFC } from 'apps/types/declaration'

const HeaderLayout: RFC = () => {
    return (
        <header>
            <div>Header</div>
        </header>
    )
}

export default HeaderLayout
