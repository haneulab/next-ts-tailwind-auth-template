import type { IFooter } from './Footer.types'

const Footer = (props: IFooter) => {
    return (
        <footer className="p-16 text-center border-t bg-slate-50">
            <code>Nextuse NextAuth</code>
        </footer>
    )
}

export default Footer
